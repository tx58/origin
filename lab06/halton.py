"""
This is try to show the basics of Halton draw.

Author: Tianli Xia
Date: Oct 1th, 2018
"""

class halton(object):

    def __init__(self, number):
        self.number = number
        self.sequence = [0]

    def generator(max):
        """
        The generator of halton sequence

        input max: how long a sequence we want
        return: a list containing max elements
        """
        temp=[]
        t=0
        while max<3^(t)
            while k<self.number:
                for a in self.sequence:
                    c.append()= a+ k/self.number^(t)
                k= k+1
            self.sequence=temp
            t=t+1
        self.sequence=self.sequence[:max]
        return self.sequence
